<!DOCTYPE html>
<html lang="it">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Dashboard - Associazione Bike and Hike</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />
  <style>
    body {
      min-height: 100vh;
      overflow-x: hidden;
    }
    .sidebar {
      background-color: #f8f9fa;
      border-right: 1px solid #dee2e6;
      height: 100vh;
      padding: 1rem;
      position: fixed;
      width: 250px;
      transition: transform 0.3s ease-in-out;
      z-index: 1030;
      transform: translateX(0);
    }
    .sidebar.collapsed {
      transform: translateX(-250px);
    }
    .content {
      margin-left: 250px;
      padding: 1.5rem;
      transition: margin-left 0.3s ease-in-out;
    }
    .content.expanded {
      margin-left: 0;
    }
    .sidebar .menu-title {
      font-weight: 600;
      text-transform: uppercase;
      color: #212529;
      margin-top: 1rem;
      margin-bottom: 0.5rem;
      border-bottom: 2px solid #dc3545;
      padding-bottom: 0.25rem;
    }
    .sidebar a {
      color: #212529;
      text-decoration: none;
      display: block;
      padding: 0.25rem 0.5rem;
      text-transform: uppercase;
      font-size: 0.85rem;
    }
    .sidebar a:hover {
      background-color: #f1f1f1;
      border-radius: 4px;
    }
    .menu-toggle {
      font-size: 1.5rem;
      cursor: pointer;
      color: #dc3545;
      position: fixed;
      top: 1rem;
      left: 1rem;
      z-index: 1040;
      display: none;
    }
    @media (max-width: 768px) {
      .sidebar {
        transform: translateX(-250px);
        position: fixed;
      }
      .sidebar.collapsed {
        transform: translateX(0);
      }
      .content {
        margin-left: 0;
      }
      .content.expanded {
        margin-left: 250px;
      }
      .menu-toggle {
        display: block;
      }
    }
  </style>
</head>
<body>

  <div class="menu-toggle">&#9776;</div>

  <!-- sidebar senza la classe "collapsed" -->
  <nav class="sidebar" id="sidebarMenu">
    <div class="search mb-3">
      <input type="text" class="form-control" placeholder="Search" />
    </div>

    <div>
      <div class="menu-title">Menu</div>

      <a href="/home">Home Page</a>

      <a class="d-flex justify-content-between align-items-center" data-bs-toggle="collapse" href="#clientiMenu" role="button" aria-expanded="false" aria-controls="clientiMenu">
        Clienti <span class="caret">&#x25BC;</span>
      </a>
      <div class="collapse" id="clientiMenu">
        <a href="/clienti/nuovo" class="ps-4">Nuovo Cliente</a>
        <a href="/clienti/ricerca" class="ps-4">Modifica Cliente</a>
        <a href="/clienti/lista" class="ps-4">Lista Clienti</a>
        
      </div>

      <a class="d-flex justify-content-between align-items-center" data-bs-toggle="collapse" href="#prodottiMenu" role="button" aria-expanded="false" aria-controls="prodottiMenu">
        Prodotti <span class="caret">&#x25BC;</span>
      </a>
      <div class="collapse" id="prodottiMenu">
        <a href="/prodotti/nuovo" class="ps-4">Nuovo Prodotto</a>
        <a href="/prodotti/lista" class="ps-4">Lista Prodotti</a>
      </div>
    </div>
  </nav>

  <main class="content" id="contentArea">
    <h1>Benvenuto nella Home</h1>
    <p>Qui puoi gestire clienti, prodotti e altre funzionalità dell’associazione MTB.</p>
  </main>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  <script>
    const sidebar = document.getElementById('sidebarMenu');
    const content = document.getElementById('contentArea');
    const toggle = document.querySelector('.menu-toggle');

    toggle.addEventListener('click', () => {
      sidebar.classList.toggle('collapsed');
      content.classList.toggle('expanded');
    });
  </script>

</body>
</html>

