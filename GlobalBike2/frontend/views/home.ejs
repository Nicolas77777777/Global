<!DOCTYPE html>
<html lang="it">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Dashboard - Associazione MTB</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />
  <style>
    body {
      min-height: 100vh;
      overflow-x: hidden;
    }
    .sidebar {
      background-color: #f8f9fa;
      border-right: 1px solid #dee2e6;
      height: 100vh;
      padding: 1rem;
      position: fixed;
      width: 250px;
      transition: transform 0.3s ease-in-out;
      z-index: 1030;
      transform: translateX(0); /* visibile di default */
    }
    .sidebar.collapsed {
      transform: translateX(-250px);
    }
    .content {
      margin-left: 250px;
      padding: 1.5rem;
      transition: margin-left 0.3s ease-in-out;
    }
    .content.expanded {
      margin-left: 0;
    }
    /* Link stile menu */
    .sidebar .menu-title {
      font-weight: 600;
      text-transform: uppercase;
      color: #212529;
      margin-top: 1rem;
      margin-bottom: 0.5rem;
      border-bottom: 2px solid #dc3545; /* rosso come immagine */
      padding-bottom: 0.25rem;
    }
    .sidebar a {
      color: #212529;
      text-decoration: none;
      display: block;
      padding: 0.25rem 0.5rem;
      text-transform: uppercase;
      font-size: 0.85rem;
    }
    .sidebar a:hover {
      background-color: #f1f1f1;
      border-radius: 4px;
    }
    /* Icona hamburger */
    .menu-toggle {
      font-size: 1.5rem;
      cursor: pointer;
      color: #dc3545;
      position: fixed;
      top: 1rem;
      left: 1rem;
      z-index: 1040;
      display: none; /* nascosta su desktop */
    }
    /* Responsività */
    @media (max-width: 768px) {
      .sidebar {
        transform: translateX(-250px);
        position: fixed;
      }
      .sidebar.collapsed {
        transform: translateX(0);
      }
      .content {
        margin-left: 0;
      }
      .content.expanded {
        margin-left: 250px;
      }
      .menu-toggle {
        display: block;
      }
    }
  </style>
</head>
<body>

  <!-- Icona hamburger per aprire/chiudere menu -->
  <div class="menu-toggle">&#9776;</div>

  <!-- Sidebar -->
  <nav class="sidebar collapsed" id="sidebarMenu">
    <div class="search mb-3">
      <input type="text" class="form-control" placeholder="Search" />
    </div>

    <div>
      <div class="menu-title">Menu</div>

      <a href="/home">Home Page</a>

      <a class="d-flex justify-content-between align-items-center" data-bs-toggle="collapse" href="#clientiMenu" role="button" aria-expanded="false" aria-controls="clientiMenu">
        Clienti <span class="caret">&#x25BC;</span>
      </a>
      <div class="collapse" id="clientiMenu">
        <a href="/clienti/nuovo" class="ps-4">Nuovo Cliente</a>
        <a href="/clienti/lista" class="ps-4">Lista Clienti</a>
      </div>

      <a class="d-flex justify-content-between align-items-center" data-bs-toggle="collapse" href="#eventiMenu" role="button" aria-expanded="false" aria-controls="eventiMenu">
        Eventi <span class="caret">&#x25BC;</span>
      </a>
      <div class="collapse" id="eventiMenu">
        <a href="/eventi/nuovo" class="ps-4">Nuovo Evento</a>
        <a href="/eventi/lista" class="ps-4">Lista Eventi</a>
      </div>

      <a class="d-flex justify-content-between align-items-center" data-bs-toggle="collapse" href="#tipologicheMenu" role="button" aria-expanded="false" aria-controls="tipologicheMenu">
        Tipologiche <span class="caret">&#x25BC;</span>
      </a>
      <div class="collapse" id="tipologicheMenu">
        <a href="/tipologiche/nuovo" class="ps-4">Nuova Tipologica</a>
        <a href="/tipologiche/lista" class="ps-4">Lista Tipologiche</a>
      </div>

      <a href="/logout">Logout</a>
      <a href="/test">Test</a>
    </div>
  </nav>

  <!-- Content -->
  <main class="content" id="mainContent">
    <h1>Dashboard Associazione MTB</h1>
    <p>Benvenuto nel gestionale eventi mountain bike.</p>
    <!-- Qui potrai aggiungere contenuti dinamici -->
  </main>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  <script>
    const sidebar = document.getElementById('sidebarMenu');
    const mainContent = document.getElementById('mainContent');
    const toggleBtn = document.querySelector('.menu-toggle');

    toggleBtn.addEventListener('click', () => {
      // Il toggle agisce solo su schermi piccoli
      if (window.innerWidth <= 768) {
        sidebar.classList.toggle('collapsed');
        mainContent.classList.toggle('expanded');
      }
    });

    // Reset sidebar allo resize finestra
    window.addEventListener('resize', () => {
      if (window.innerWidth > 768) {
        sidebar.classList.remove('collapsed');
        mainContent.classList.remove('expanded');
      } else {
        sidebar.classList.add('collapsed');
        mainContent.classList.remove('expanded');
      }
    });

    // Imposta stato iniziale corretto
    if (window.innerWidth <= 768) {
      sidebar.classList.add('collapsed');
    } else {
      sidebar.classList.remove('collapsed');
    }
  </script>

</body>
</html>
